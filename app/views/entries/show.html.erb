<%= render @entry %>

<div>
  <%= link_to "Edit this entry", edit_entry_path(@entry) %> |
  <%= link_to "Back to entries", entries_path %>
  <%= button_to 'Delete', entry_path(@entry), method: :delete, form: { data: { turbo_confirm: "Are you sure?" } }, class: 'button is-danger' %>
</div>
